# Make sure VT is supported and enabled, and KVM module is loaded
- include: kvm-vt.yml
# Download and build gtest if not existing
- include: gtest.yml
# Download and build bear if not existing
- include: bear.yml
# Download and build libwebsockets if not existing
- include: libwebsockets.yml
